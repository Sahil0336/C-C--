#include<stdio.h>
int main()
{
   int arr(3);
   int i,n;
   printf("Enter number of elements: ");
   scanf("%d",&n);
   for ( i = 0; i < n; i++)
   {
    
    scanf("%d",&arr[i]);
   }
   for ( i = 0; i < n; i++)
   {
    if (/* condition */)
    {
        /* code */
    }
    
   }
      
}